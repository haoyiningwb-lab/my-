# 2026-03-23

- 用户反馈业务日报问题：3-22 日内容应置顶但当前在最下方，且 3-22 缺少“业务速览”表格。
- 已在本地脚本 `scripts/daily_report_generator.py` 中补充 `render_report_table_values()`，明确表格字段顺序，便于后续用 Feishu Docx table 正式写入业务速览。
- 已按用户提供的飞书文档链接，修正 3-22 日排序并补齐、刷新 3-22 的业务速览表。
- 用户新增规则：需要每日给 4 名员工派发学习任务，并持续跟踪。
- 用户再次强调：每次新增任务后，都要更新桌面两份固定备份文件。
- 用户明确授权：这类固定动作由代理自行补上，不用再次询问。
- 今日已补派员工学习任务：
  - analyst / 小树：复盘日报自动化指标结构，输出“异常识别优先级”草案。
  - cleaner / 小清：整理日报表格字段口径，输出字段清洗与校验清单。
  - policy-writer / 小文：沉淀《日报自动化流程 SOP》初稿。
  - prompt-qa / 小规：设计“日报异常描述质量检查清单”。
- 新增任务：开始搭建数据看板第一版，技术路线选择 Streamlit + Plotly，数据源接本地 xlsx 落地文件，优先实现总览、趋势、异常三块。
- 用户提供历史 Streamlit 看板链接，要求参考其风格重做当前看板，并优先修正“数据不准确”问题。
- 已将看板数据解析逻辑从按业务硬编码列号改为按表头识别字段，新增数据新鲜度判断，并重做页面布局为总览趋势 / 业务钻取 / 异常排查三层结构。
- 用户确认接受“通用数据层 + BI 可导入出口”方案。
- 已新增 BI 导出脚本与数据模型文档，成功导出 5 份标准化文件：fact_daily_metrics / dim_business / biz_summary_latest / fact_alerts / biz_trend_30d。
- 已继续补充 DuckDB 导出与 Metabase 接入说明，成功生成 `dashboard/exports/dashboard.duckdb`，包含 5 张标准化表。
- 已进入下一步，补齐 SQLite 导出、Metabase 仪表盘模板与可直接复用 SQL 查询模板，成功生成 `dashboard/exports/dashboard.sqlite`。
- 用户进一步反馈看板问题：表头被遮挡、日期需中文展示、图表需要全量数据点标签、左栏需要进一步美化和增强功能。
- 已针对上述问题重做 `dashboard/app.py`：增加顶部留白、统一中文日期、为趋势图增加数据点标签、扩展左侧栏为筛选中心并增加状态筛选/仅看异常等功能。
- 用户继续指出优化方向：日期改成“2月21日”这种短格式，总进审量标签错误，业务钻取也要加标签，示例图和图例尽量中文，左栏仍需继续美化。
- 已继续调整 `dashboard/app.py`：将横轴日期改为短中文格式、总进审量标签改成 compact_num_label、业务钻取两张图补上数据标签并改中文图例，同时进一步优化左栏样式和筛选交互。
- 用户给出新的示例图，要求所有折线图统一为“底部中文短日期 + 每个点显示标签 + 面积填充”的展示风格。
- 已继续调整 `dashboard/app.py`：新增统一折线/面积图样式函数，并将总进审量、平均推审率、平均违规率、业务钻取两类趋势图统一到同一视觉风格。
- 用户最新反馈：所有图统一保留折线图，不要面积图；同时标题区域太密，需要放松图表标题和上边距。
- 已继续调整 `dashboard/app.py`：移除所有面积填充，恢复纯折线 + 点标签，并统一下调标题密度与图表顶部间距。
- 用户确认要做成“每天 10 点自动同步、所有线上访问者都能看到更新后内容”的线上成品。
- 已新增 `dashboard/app_cloud.py`、`dashboard/sync_to_repo.sh`、`dashboard/DEPLOY-ONLINE.md`、`dashboard/launchd/com.haoyining.dashboard-sync.plist`，形成线上部署版与每日 10 点自动同步模板。
- 已检查到 `launchctl` 中存在 `com.haoyining.dashboard-sync`，说明每日自动同步任务已挂载；但目前日志为空，说明还未实际跑到下一个触发时点。
- 用户反馈线上版与本地版界面不一致，已将 `dashboard/app_cloud.py` 升级为更接近 `dashboard/app.py` 的完整版布局与交互，同时继续保留其“读取仓库内导出数据”的线上模式。
